on:
    push:
      branches:
        - "**"

jobs:
    docs:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-node@v4
          with:
            node-version: 22
        - run: |
            cd docs
            yarn install --no-lockfile
            yarn build